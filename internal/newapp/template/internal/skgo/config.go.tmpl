// Package {{.BindingsPackage}} holds skgo's generated implementation. Regenerate it
// with `go generate ./...`.
//
// `go tool skgo` builds the generator from the module cache, so nothing here
// needs skgo to be a writable checkout.
package {{.BindingsPackage}}

//go:generate go tool skgo generate --web {{.BindingsWeb}}

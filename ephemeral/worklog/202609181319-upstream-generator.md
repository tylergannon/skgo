decision: skgo new will invoke VitePlus with an exact registry-resolved sv 1.x version; both starting points use sv's minimal template, and the skgo add-on receives the explicit minimal or examples choice instead of inferring it.
decision: the native sv add-on is the sole writer of package.json and vite.config frontend integration; Go writes only Go-owned project files and the examples remote implementation.
decision: Storybook and Vitest are selected in the upstream sv create operation; generator success additionally requires their concrete scripts/config/spec/story artifacts because upstream Storybook failure can be swallowed by sv.
friction: sv community add-on arguments split on the first equals sign, so the adapter's <= release range cannot safely be used as the add-on package spec -> resolve the newest published adapter version at or below the Go module version and invoke that exact package; the add-on installs the same exact adapter.
decision: Sverdle is deferred because its server load, form actions, form-data handling, and cookies require runtime work outside this generator; examples will demonstrate existing Go remote query and command behavior instead.
friction: sv create treats --add as variadic, so a target path placed after add-on selections is consumed and the CLI prompts even under the VitePlus non-interactive wrapper -> pass the explicit web target before --add and keep a unit assertion on that ordering.
friction: sv still asks for trust confirmation for a community add-on in the wrapped non-interactive create path -> pass sv's supported --no-download-check flag after the variadic add-on list; the package/version choice remains explicit and generator-owned.
friction: VitePlus --approve-builds runs after sv returns and cannot approve esbuild inside Storybook's nested pnpm dlx; pnpm's broad environment setting is ineffective there -> run the skgo add-on before Storybook and have that sole frontend integration owner write pnpm's supported allowBuilds entry for esbuild.
correction: pnpm dlx uses a separate temporary environment and ignores the generated project's allowBuilds map; pnpm 12 exposes --allow-build on dlx itself -> invoke Storybook's same upstream create-storybook installer directly with --allow-build esbuild, then let the generated project's local VitePlus install its declared dependencies. Do not carry the ineffective workspace mutation.
friction: pnpm --dir web dlx installs in the requested context but create-storybook still inherits the wrapper's root working directory and cannot find package.json -> set the subprocess working directory to web instead of relying on pnpm's directory option.
friction: create-storybook auto-detection calls npm config even in a VitePlus pnpm project, and npm rejects the project because devEngines requires pnpm -> pass create-storybook's supported --package-manager pnpm option explicitly.
friction: create-storybook still uses npm for a read-only registry fallback after --package-manager pnpm, and npm enforces the project's pnpm devEngine before config get -> scope npm_config_force=true to the create-storybook subprocess so that lookup warns instead of exits; the installer and all dependency work remain explicitly pnpm/VitePlus-owned.
friction: running web/node_modules/.bin/vp install from the Go project root makes VitePlus detect npm because the package manifest is under web -> run the project-local VitePlus executable with web as its working directory.
friction: pnpm install has no --allow-build flag; VitePlus correctly installs Storybook's declared dependencies but reports esbuild as pending -> follow VitePlus install with non-interactive pnpm approve-builds esbuild --yes, which records the narrow approval and executes the pending postinstall.
correction: VitePlus preserves an add-on-created allowBuilds.esbuild entry while writing its pnpm workspace catalog, and its project install reads that policy -> retain the native add-on's narrow workspace entry for project installs; keep dlx --allow-build separately because dlx has an isolated temporary environment.
friction: the upstream Vitest add-on writes a test script through npm run, but VitePlus's devEngines rejects npm in the pnpm project -> after Vitest runs, the native skgo add-on changes only that runner spelling to pnpm while preserving the upstream test command.
friction: the examples page awaits a remote query during render and Svelte rejects top-level template await without experimental.async -> enable the compiler option through the add-on's upstream svelteConfig helper alongside remoteFunctions.
decision: the native installer add-on is a separately published @skgo/sv package; @skgo/sveltekit-adapter remains runtime-only. Both are sparse packages versioned from the same Go release tag, so selecting each package's newest exact version not newer than the running skgo release gives a deterministic compatible pair without requiring matching publication histories.
friction: Go's embed package excludes dot-prefixed files when embedding a directory, so a .gitkeep did not create the generated web/build placeholder and a new app failed to compile before its first frontend build -> use a non-hidden placeholder that the generated root .gitignore still excludes.
friction: the Go dev server reads the adapter manifest before it overlays VitePlus's dev manifest, so a placeholder-only build directory compiles but cannot serve the first request -> generation must run the project-local VitePlus build after Go bindings exist, before announcing success.
friction: pnpm passes script arguments directly and Storybook 10 treats a literal separator as an extra positional argument -> the generated recipe must use `pnpm storybook --host 127.0.0.1`, without npm's `--` spelling.
friction: the existing bindings generator refused a source tree with no Go endpoint declarations, so the intentionally behavior-free minimal starter could not complete -> emit empty Go registration lists and an empty adapter comparison manifest when no source files exist; do not invent a placeholder handler or demo.
correction: create-storybook's registry fallback is unnecessary when pnpm dlx installs both exact create-storybook@10.6.0 and @storybook/sveltekit@10.6.0 packages -> use the dual-package dlx environment so framework templates resolve locally and remove npm_config_force entirely.
friction: a root .gitignore exception cannot override sv's nested web/.gitignore, and the initial adapter build cleans web/build after Go first writes the placeholder -> the native add-on owns the nested ignore exception and Go restores its embed placeholder after the initial frontend build; prove both with an actual commit and clone.
decision: the Storybook acceptance subject is the real component stories produced by the upstream installer for each generated starter; requiring those stories to exercise skgo runtime behavior would add demo functionality to minimal and create a second integration concern not present in the Gherkin contract.
decision: bootstrap @skgo/sv manually only after stamping the real skgo version and inspecting the packed tarball; after that first manual publication succeeds, configure and verify npm trusted publishing before letting the release workflow own later publications.
friction: QueryEscape uses a literal plus for spaces while sv also uses plus as its option separator -> percent-encode spaces and literal plus signs before handing option values to sv, then decode both adapter and application name in the add-on.
correction: a Kit command's updates() promise retains the command output type; the example command returns Status rather than a string -> render result.message so the form visibly shows the Go command result while the refreshed status query demonstrates the updated counter.
correction: a non-hidden generated placeholder survived the initial clone but the adapter replaces it with .gitkeep on every documented build -> use the adapter's existing .gitkeep contract throughout and prove the three owners name the same file.
correction: never populate an independent-validator artifact by copying an earlier review; verify reviewer session identity and provenance, not merely that the file is nonempty.
friction: Codex's command sandbox hides host npm credentials and blocks registry DNS -> run every authentication, build, pack, inspection, and publish step for a release in the host context.
decision: bootstrap @skgo/sv@0.5.0 manually from this candidate branch before merge; the merge must retain a feat title so its computed Go release remains v0.5.0.
decision: @skgo/sv@0.5.0 was manually published as the initial public package; a fresh-cache download was byte-identical to the inspected tarball, and npm trusted publisher 23eaf615-d90d-4ed9-bc05-80c5f6024ba6 binds direct and staged publishing to tylergannon/skgo release.yml.
friction: npm's registry metadata endpoint briefly retained a pre-publication 404 after the package page and access API showed the successful public release -> require a fresh consumer cache and byte comparison before calling an npm bootstrap complete.
friction: sv installs a file: add-on as a symlink inside its own shared pnpm-store directory, extracts later registry add-ons through that link, and only accepts a file: directory or the hardcoded npmjs registry -> skgo new packs a file: add-on with pnpm and hands sv the unpacked copy under the user cache dir; never hand sv a path inside a checkout.
friction: Node resolves a linked add-on's `sv` import from the link's real path, so the staged copy fails to import without its own peer (the live checkout only worked because internal/sv/node_modules exists) -> run pnpm install --prod --ignore-workspace in the stage. The stage is a fixed persistent path on purpose: a dangling link would break the next registry extraction.
friction: an older link from sv's shared pnpm store to this worktree's internal/adapter predated the separate add-on; nothing extracts that package name there any more, and the exact stale link was removed before final validation.
decision: the generated dev recipe uses the files it actually needs as its sentinels (web/node_modules/.bin/vp, web/build/skgo.manifest.json) rather than a marker file; a clone's `just test` still needs `playwright install chromium`, which the generated README now states.

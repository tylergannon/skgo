# Agent toolkit

correction: Current research must supersede the older TSGo success note: the independent fresh fixture found plain --tsgo missing a #lib component prop error and experimental mode emitting incorrect authored paths. Classic official checking remains the default until parity is demonstrated.
friction: svelte-check 4.7.6 can print failure for missing native dependency and exit zero -> integration must require completed machine diagnostics and retain behavioral regression fixtures.
decision: Full Svelte component lint remains ESLint plus its Svelte plugin; Oxlint script parsing and alpha plugin support are not full component linting. Oxfmt integrates Svelte through a Prettier-backed path; do not transfer JS speed claims to Svelte.
decision: User explicitly requested this sprint plan and Gimble execution. Use installed implement workflow with ordered outcome strings and independent validation; retain default role models and keep commits/merge outside the implementation run.
correction: User wants broader Go correctness and application wiring checks while the run continues. Add a curated error/context/resource preset and explicit deeper dependency/build checks; retain concrete cross-language diagnostics as the differentiator and classify unprovable dynamic relationships honestly.
correction: The user clarified that ambition means skgo developer advice encoded as Go analyzers, not an expanding generic linter catalog. Replaced the prior expansion with type-aware, source-backed advice candidates and bad/good acceptance pairs; generic extra/security presets are not mandatory sprint work.

friction: A native POST response can contain correct profile and receipt strings only in Kit's hydration script while its rendered DOM is blank. Assert the exact data-testid elements from the raw response in a JavaScript-disabled browser context before claiming native SSR.

friction: A fault probe that changed only the authored page.server.go still passed because the example server compiles the generated internal/skgo/links copy. Mutation probes must alter the code the running Go server executes, restore both copies exactly, then rerun green evidence.

decision: Kit next.27 reports an enhanced no-data success as HTTP 200 with ActionResult status 204, and its client sets page.status to 204. The native document and a later GET have page.status 200; assert these separately.

friction: Rendering a native action error boundary before encoding the branch's Go load values produced a 500 from goja's devalue serializer, hiding the intended 403/500. Run the same branch encoding as a normal document before serving the nearest error page.

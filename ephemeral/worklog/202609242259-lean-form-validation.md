correction: Issue #183's no-success-screenshot language was superseded in session: keep inspectable outcome screenshots for retained Gherkin scenarios. The problem was the number of duplicate journeys and per-assertion captures, not the screenshot rule.
correction: Production and development each need the full selected suite because the development renderer is part of the product. Run both suites in parallel on separate checkouts so source-edit scenarios cannot collide.
friction: The extra native response-header journey edited its form before development hydration, allowing bound state to overwrite Grace with Ada. Assert the header on the existing Grace save after its hydration synchronization instead.
friction: Removing per-assertion capture exposed that Playwright's `shot` fixture was lazy, so a scenario without an explicit shot had no success image; make it auto and leave a final frame when none was requested.
friction: The cross-page scenario spent four screenshots on its sender form and none on the destination it claimed; capture the destination outcome instead.
friction: The Optional Form browser scenario asserts an absolute operation count from the Go server's in-memory fixture. Reusing the server for a second full suite made the expected first operation become eight. Start each complete run with a fresh server; the two CI matrix jobs already provide that isolation.
friction: A green dev run produced a genuinely blank screenshot for a no-script `ssr = false` shell, whose only outcome is invisible. Drop that nonvisual browser scenario. The static 500 case also took three byte-identical screenshots around one visible outcome; generic status and absence steps need no extra frame. An image viewer briefly displayed two of those identical images as white, so compare source bytes before calling an image blank.

friction: Kit's generated dev node files are rewritten non-atomically and stale high-numbered files can remain after route removal -> build the render node table directly from Kit's single create_manifest_data snapshot.
friction: A change cursor that collapses consecutive events for the same path loses edit-then-restore cycles -> every filesystem event must advance the cursor.
decision: refresh live routes, load-node routing, endpoint routing, render-node options, app.html, and error.html as one locked snapshot before rendering a document.
decision: collect dev CSS only after rendering the branch because Vite discovers lazy component dependencies while the render modules are evaluated.
friction: Playwright screenshot commands wait behind an open streamed navigation -> retain compositor screencast frames so pending UI states can be captured without waiting for the response to finish.
correction: dev SSR is the product path, not a separate shell mode; one Gherkin suite must make the same document claims against dev and production.
friction: fast compositor screenshots exposed that a refreshed query can render before an enhanced form's success callback resets its controls -> synchronize a second submission on the observable reset, not on the refreshed row alone.
friction: rerunning live-query scenarios against a long-lived server leaves older rows with the same fixture text -> wait for one additional matching occurrence before comparing the independent list and stream counts.
compatibility: keep NewDevPages' original four-argument call valid; accept the endpoint registry as an optional trailing argument for live endpoint-route refresh.
proof: a fresh dev process and the final production build each passed the same 147 Gherkin scenarios; all 281 distinct screenshots were visually inspected, with the no-script and live-source frames also checked at full resolution.
friction: CI used Vite's document root as its readiness probe; now that dev SSR is active, that route correctly executes the generated Go-only server-load stub and returns 500 -> probe the adapter control endpoint and let `just dev` own both Vite and Go.
integration: main gained #98's earlier live-routing slice while this branch was under proof; the completed branch already supersets its behavior and passes route-addition scenarios, so main was recorded as an ancestor without replacing the proved implementation tree.
friction: CI's slower browser could observe SSR stream frame 1 before hydration opened the subscription, then the scenario counted that first stream request as a refetch -> observe the stream request before marking the zero-request baseline.
friction: Kit may invalidate its generated client graph while the route-addition scenario is navigating, making Vite abort it as `net::ERR_ABORTED` or interrupt it with the reload navigation -> retry only those two transient forms and still require a successful document with the expected page.
friction: Vite may replace the server's `style[data-sveltekit]` after hydration, so DOM ownership of that tag is timing-dependent -> assert the CSS bytes in Go's response and the resulting computed style in the browser.
friction: the nonce-CSP live-update scenario had the same SSR-frame-before-subscription race as the dedicated live scenario -> require the observed stream request before mutating the other tab.

correction: The first npm package release carried the pre-registry GitHub-subdirectory install command in its package README. A successful package publish is not enough when the public installation surface still advises an obsolete source path.
decision: Publish a corrected README in v0.2.8 and remove the one-time OTP recovery trigger now that the direct scoped-token publish has succeeded.

# Worklog — Sprint 002 planning (2026-09-05)

decision: planning runs in worktree `.claude/worktrees/sprint-002-plan`; the gitignored token cache is reached through an untracked symlink `ephemeral/inspiration -> /Users/tyler/src/skgo/ephemeral/inspiration` so the drafting agents can resolve index citations without leaving the worktree (Sprint 001 planning wrote into the root checkout instead).
decision: Sprint 002 scope proposed as `query` + `command` only, hand-written throwing stub, no codegen; live/batch/form/prerender/loads deferred. Polytype inclusion and the typed-bridge shape left as open questions for the drafts and the interview.
doc_bug: `ephemeral/semantic-index/sources/libs/polytype.md` and build plan §3 still say polytype rc.5 / `go-gen-jsonschema` import path; rc.9 facts live in `ephemeral/inspiration/reference/polytype/README-LOCAL.md` -> refresh the leaf when the sprint that adopts polytype lands.
correction (Tyler, 2026-09-05 22:20): scope is `query`, `query.live`, and `command` remote functions, actually seen working; proof = Playwright BDD (Gherkin, simple scenarios) against a PRODUCTION build; then squash-merge. No gold plating, no over-polish: working and 90-95% complete is the bar. Do not get caught up planning and planning: find out how to build it, build it, prove it, fix it.
correction (Tyler): planning itself was not cancelled — I wrongly killed the three drafting agents. Run planning fast, then delegate execution to Opus subagents and monitor them; the orchestrator stays smart and hands-off.
correction (Tyler, 2026-09-05 22:35): I burned the whole context window reading semantic-index leaves in the orchestrator before writing any plan. The index is for peeking at citations; subagents read, the orchestrator receives conclusions. Recorded as memory `orchestrator-does-not-read`.

## Sprint 002 build

decision: cross-agent contract pinned up front — module `src/lib/todos.remote.ts` (hash `worolc`) exporting `getTodos` (query), `getTodo` (query, string id), `addTodo` (command), `watchCount` (query.live); seed todos `t1`/`t2`.
discovery (captured from the built client in Chromium, not from docs): `GET /_app/remote/worolc/getTodos` omits `payload` entirely when the arg is undefined; `getTodo` sends `?payload=WyJ0MiJd`; `addTodo` POSTs `{"payload":...,"refreshes":["worolc/getTodos/"]}`. The refresh key ends in a trailing `/` with an EMPTY raw payload — split on the LAST `/`. The command response must return the refreshed value in `q["worolc/getTodos/"].v` with `r:true` or the list never updates. One request per add, so the single-flight assertion is real.
friction: `vp dev` is broken on this toolchain (vite-plus 0.3.0 + kit 3.0.0-next.25: "The configured Vite SSR environment must be a RunnableDevEnvironment") -> prod-only proof is forced, not chosen; revisit when vite-plus updates.
doc_bug: `example/web/src/routes/+layout.ts` sets `ssr = false`, so remote calls only ever come from the browser and there is no hydration payload to seed a live query. Worth knowing before anyone plans SSR-side remote handling.
friction: `example/web/build/` is gitignored but embedded by `dist.go` -> `vp build` must run before `go vet ./example/...`.
friction: `vp check` with fmt fails on pre-existing `example/e2e` files (oxfmt disagrees with committed style) -> use `vp check --no-fmt`.
correction: SendMessage is disabled in this session, so a running subagent cannot be given new facts mid-flight. Front-load everything into the launch prompt, or fold late discoveries in via a follow-up agent.

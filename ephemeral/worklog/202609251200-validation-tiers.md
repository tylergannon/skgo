correction: Issue #183 said "no screenshots on success"; the codex agent's worklog on codex/issue-183-lean-form-validation records overriding that in-session because AGENTS.md demanded per-assertion screenshots. Doctrine text outranked the issue. When the owner's intent is in the issue, the doctrine is the thing to change, not the issue.
correction: Toolchain tests pay for the same process per assertion: the skgo binary is built 4 times per full run, the example is sandbox-copied 4 times, svelte-check runs 5 times, and one test does 8 full packages.Load passes. Owner's rule: run the expensive command the minimum number of times and assert many times against its captured output.
friction: My first draft nominated whole feature files (transport, batch, emptyarray) for removal from the browser suite by a per-file heuristic. Codex review showed each holds a scenario whose claim needs kit's client (a class rebuilt with methods, four calls coalesced by client navigation). Classify per scenario by its complete claim, never per file.
friction: Codex's adversarial review (gpt-6-sol, high) upheld 4 of 7 objections and misread one: it treated an agent's worklog as the owner's decision. Verify who wrote a "recorded decision" before deferring to it.
correction: Tyler rejected four tiers as insane and the "tier 1 takes minutes" framing. The rethink starts from failure classes and their cheapest detector, not from the existing suite. Result: two commands (`just test` in 15s, `just e2e` on behaviour change and main) and one human look via gimble. Never propose a fast lane around a slow test; make the test fast.

friction: the generated-app acceptance test reached Go compilation only after its full build gesture, so it did not state that an untouched frontend tree must compile independently -> keep a focused pre-Node compile test beside the full application test
decision: preserve /bin/ because it already ignores the built application; narrow web/build/ to web/build/* plus a .gitkeep exception so the single embed placeholder is trackable while bundle output stays ignored
friction: an isolated scaffold cannot run go generate before go mod tidy because its tool dependencies have no go.sum entries; this is already the documented build order -> the pre-frontend compile proof must retain go mod tidy without introducing Node or Vite
friction: the generator-specific proof passed while just vet still failed on the unbuilt example's identical empty embed pattern -> keep the repository example aligned with the generated app so a fresh skgo checkout also compiles
friction: the adapter recursively clears build/ before writing output, which deleted the tracked placeholder during every normal frontend build -> restore .gitkeep from the adapter and assert the documented scaffold build preserves it

correction: mise may remain useful for this repository, but it must not be the only build workflow imposed on generated applications.
decision: `skgo new` will offer mise, Just, and executable shell scripts as selectable build-tool outputs; existing mise behavior remains the compatibility default.

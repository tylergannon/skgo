# Form actions proof design

correction: The user rejected treating existing devalue serialization as new work. Classic action proof must exercise the existing polytype stringify/uneval and transport paths; concrete missing capabilities go upstream to polytype with a reproducer.

friction: The current noscript project selects only feature names matching form-noscript, and curated screenshot capture selects unrelated release fixtures. New action scenarios must actually enter the disabled-JavaScript project and use all assertion captures; a new feature filename or green curated run alone does not establish either claim.

decision: The requested deliverable is a proof design for upgrading the existing example. The design defines six outcomes across three browser paths and two server modes, plus the stated edge scenarios; no runtime feature or passing proof is claimed by this planning commit.

friction: The installed agent wrapper launched codex app-server despite provider/model flags after the workdir. That process was terminated before any review was accepted. The native Claude CLI reports claude-fable-5-1 in its initialization event for review session 8463c659-3b6a-433c-9a49-e566b4ebf6b3; verify actual provider/model rather than assuming launcher arguments were honored.

correction: Native no-data action success has the same null form value and status 200 as a fresh GET. Prove it through the observed POST and the literal saved mutation; only the enhanced result carries status 204. An Examples column cannot select a disabled-JavaScript browser context.

doc_bug: The next.27 reference is an installed package without upstream tests. The matching official tag resolves to commit 090d77ff289bf844cf65403f8f5e7eec8a7bb902. Use immutable upstream test/fixture links while keeping the root reference read-only; the separate no-ssr/no-csr test apps contain no action-outcome tests at that revision.

decision: The first vertical slice must carry a transported Money result through the actual form prop and hydration. The example isolates literal seeded data in a visitor workspace whose cookie survives authentication changes, so subsequent scenarios need no shared-store reset or weakened expectations.

correction: During external review, the user asked to wait quietly for completion rather than repeatedly narrating that a verdict had not arrived.

friction: Fable's third review found that the current native remote-form CSRF check compares only the app origin. The classic-action acceptance follows Kit's trusted-origin support; copying the existing remote-form check would fail that requirement. Missing Content-Type also counts as form-shaped in Kit's CSRF check.

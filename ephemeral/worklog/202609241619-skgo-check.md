decision: skgo check must leave the caller's GOWORK selection intact; an isolated fixture can opt out in its own invocation.
friction: svelte-check --output machine gives compact text, while machine-verbose gives JSON diagnostics and a COMPLETED count; a zero exit can still accompany failure text -> parse the full completion stream.
friction: go vet -json can exit zero with diagnostics -> inspect JSON findings rather than using exit status as the verdict.
decision: read-only checking overlays current authored route Go files onto generated import addresses for Go tools, then removes the temporary overlay; the generated copies remain untouched.
decision: the approved skgo advice rules supersede the temporary broad golangci-lint preset. Keep goimports, vet and Staticcheck as the generic Go baseline.
friction: Go build prefixes legitimate type diagnostics with a `# package` banner; treat the banner as framing while treating any other unmatched output as incomplete, including when exit status is zero.
friction: generator type projection reports the generated route import address even under a read-only source overlay; remap both diagnostic text and location to the authored route file at the CLI boundary.
friction: skgo.Deferred[T] hides T behind private runtime state, so structural field walking misses Deferred[File]; inspect the generic argument before traversing fields when validating load wire values.

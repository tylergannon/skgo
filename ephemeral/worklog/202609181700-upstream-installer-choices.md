correction: `skgo new` must preserve the VitePlus and Svelte installer choice surface; the released v0.5.0 generator incorrectly forced non-interactive minimal TypeScript and replaced upstream choices with skgo-owned starter modes.
decision: Project creation remains owned by VitePlus and `sv`; mandatory skgo, Vitest, and Storybook integration is applied afterward through upstream installer mechanisms where feasible.
decision: Release this compatibility correction as a feature after a fresh installed-version proof, not merely source-level tests.
decision: The wrapper syntax must preserve an ordinary interactive `skgo new DIR` and provide an explicit non-interactive pass-through for upstream Svelte create choices; `web` remains the upstream target inside the Go project.
correction: Mandatory `@skgo/sv`, Vitest, and Storybook setup must not blindly reapply an add-on the user already selected during `sv create`; successful exit is insufficient because `sv add` can reach EOF without applying prompted work.
constraint: Svelte's `library` and `addon` templates are not runnable skgo applications. The CLI may reject them clearly; it must not claim they satisfy the application generator contract.
constraint: Do not turn this release into a Sverdle port. The supported upstream application choices and type/add-on options must either work end to end or fail explicitly without being advertised as completed.
trap: `--add NAME` alone is not unattended. sv (1.0.0-next.7 `cli/add.ts` `promptAddonQuestions`) skips its add-on picker once any add-on is named, but still asks each named add-on's unanswered options; a documented no-questions example must spell them, e.g. `tailwindcss=plugins:none` (`none` is sv's empty multiselect).
trap: `example/web` embeds `all:build`, so `cd example && go test ./...` fails `TestBuildEmbedsImmutableAssets` in a fresh worktree until `pnpm install` in `example/web` and `just build` have run. It is not a regression of whatever the branch changed.
constraint: `golang.org/x/term` in the root module reaches `example/go.mod` as indirect `x/term` and `x/sys`; `go mod tidy -diff` is clean in both modules and both build with `GOWORK=off`.
